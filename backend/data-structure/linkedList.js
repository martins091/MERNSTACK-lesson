// Linked Lists
// linked lists consist of nodes where each node contains data and reference(pointer) to the next node in the
// sequence. This strucure allows for dynamic memory allocation operaions. There are different types of linked
// lists, doubly linked lists, and circular linked lists.